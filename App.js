import react from "react";
import ReactDOM from "react-dom/client";

const Heading=<h1 className="head">hello react i am back</h1>

const root=ReactDOM.createRoot(document.getElementById("root"));

root.render(Heading);